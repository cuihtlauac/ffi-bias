<!-- TEMPLATE — copy into lab/results/<date>-<plan>-<short-sha>/record.md and fill in.
     This file is not a recorded experiment; do not link it from notebook.md. -->

# <date> — <plan> on <model> — <one-line title>

## What
<One paragraph: which probe ran (plan, language arms), how many completions,
scored how (compiler battery / judge), and the single headline result.>

## Why
<The most load-bearing section. Which pre-registered contrast does this run test
(e.g. within-OCaml perf=present vs absent)? What did you predict BEFORE the run,
and in which direction? What prior run does it build on or contradict? Link the
pre-registration / hypothesis (README §"The cue grid", or the backlog item).>

- Pre-registered prediction: <e.g. OR < 1 for perf, domain, polars; OR > 1 for recognition; placebo OR spans 1>
- Confirmatory or exploratory: <confirmatory against the above | exploratory>

## Provenance
Generated by `python3 lab/manifest.py out`. Paste the JSON here or keep it as the
sibling `manifest.json` and summarise the load-bearing fields:

- git commit / dirty: `<git_commit>` (dirty: `<git_dirty>`)
- config hash: `<config_sha256>`  (covers config.yaml, prompts.py, score_ocaml.py, api_client.py)
- provider(s): `<providers>`
- model requested → **served**: `<model_requested>` → `<model_served>`   ← record the *served* version, not just the alias
- sampling params sent: `<sampling_params>`  (e.g. temperature omitted for Opus 4.8)
- seed: `<seed>`
- OCaml oracle: `<ocaml>`   ·   Python: `<python>`   ·   pkgs sha256: `<python_packages_sha256>`
- platform: `<platform>`
- raw archive sha256: `<raw_archive_sha256>`  (verify with: re-hash the frozen out/ → same digest)

## Parameters and raw data
<Every input that varied and the resulting numbers — do NOT summarise the data away.
For this probe that means the per-cue rate table WITH intervals, not point rates:>

| cue | level | k / n | rate | Wilson 95% CI |
|-----|-------|-------|------|----------------|
|     |       |       |      |                |

Odds ratios (logistic GLM, cluster-robust by paraphrase):

| term | OR | 95% CI | p |
|------|----|--------|---|
|      |    |        |   |

Controls (must be reported every run):
- **Placebo**: OR = `<>` [`<>`, `<>`] — must span 1. <OK / WARNING>
- **Positive control (Python)**: rate = `<>` — expected LOW.
- **Judge calibration** (if judge ran): compiler-vs-judge Cohen's κ = `<>` (n=`<>`).

Raw dataset: `<N completions>` in the archived `out/` (`raw_archive_sha256` above);
`needs_manual_review` / non-compiling: `<>`.

## Analysis and open questions
<What the data means. Did the prediction hold? Replication: how does this compare to
prior recorded runs of the same contrast (run-to-run variance)? Be explicit about a
FAILURE to replicate. What this run could NOT settle (e.g. Python-specific prior vs.
generic FFI caution) and which follow-up / backlog item picks it up.>
