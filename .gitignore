# Python
__pycache__/
*.py[cod]

# Experiment outputs (generated by the pipeline)
/out/
/out_*/

# Local Python venv (system Python is PEP-668 externally managed)
/.venv/

# Secrets — API keys, auto-loaded by api_client._load_dotenv(). NEVER commit.
# (.env.example is a committable, secret-free template.)
/.env

# Local-only — funding application draft, never published
/APPLICATION.md
